# 精度設計書 — スマホカメラだけで「最強」のスピードガンを作る

目標: **子供が持っている普通のスマホ1台**で、市販スピードガン(±1km/h)に匹敵する精度を出す。
特別なハードウェア・課金・高級機は前提にしない。

---

## 1. 誤差の構造を分解する

球速 = 距離 ÷ 時間。誤差は4つの独立した源から来る。

| 誤差源 | 現状の大きさ | 内容 |
|---|---|---|
| **E1: 距離の定義ズレ** | **最大 -10%(系統誤差)** | 18.44mはプレート〜ベース間。実際のリリースは体の前方約1.8〜2mで、捕球はベース後方。実飛行距離は16.5〜17m。今は「18.44m飛んだ」として計算しているので、**常に速く出る方向にズレる** |
| E2: 時間分解能 | ±1.5〜3%(ランダム) | 30/60fpsのフレーム刻み。多点回帰で1/√n に低減済みだが、サンプル数が少ないと残る |
| E3: 空間スケールの歪み | ±1〜3%(系統+ランダム) | px→m換算が「画面内で一様」と仮定している。カメラが軌道の真ん中・真正面にないと、軌道の左右でスケールが変わる。レンズ歪み・ボール検出の量子化誤差も含む |
| E4: 検出の質 | 状況依存 | 手ブレ、背景の動き(人・鳥・旗)、モーションブラー、逆光。誤検出すれば数字ごと壊れる |

**重要な順は E1 > E3 > E2 > E4。** 今いちばん大きいのはフレームレートではなく「距離の定義」。

---

## 2. 設計の核心: 「距離を入力させない」

E1とE3を同時に潰す方法は一つ — **既知の長さを画面内から取って、px→mの換算をその場でキャリブレーションする**。

### キャリブレーション方式(横からモード)

1. ユーザーは計測前に、画面上で **2つの基準点** をタップする
   - 基準点A: ピッチャープレート / 基準点B: ホームベース(または塁間・メジャーで測った2点)
   - この2点間の実距離は規格で決まっている(18.44m / 16m / 13.11m / 塁間27.43m)
2. アプリはこの2点で **px→mのスケールと軌道方向** を確定する
3. ボール追跡は**実際にボールが動いた区間**を実距離に換算する
   - リリースが前方1.8mでも、捕手が深く構えていても、**ボールが実際に飛んだ距離**で計算される
   - → E1(距離定義ズレ)が原理的に消える

### 透視補正(ホモグラフィ)

カメラが軌道の真横・中央に立てない場合(現実にはほぼ毎回)、地面上の長方形(プレート、ベース、ライン交点など4点)をタップしてもらえば、ホモグラフィ行列で地面座標系に逆投影できる。
MVPでは**2点キャリブレーション+「真ん中に立ってね」ガイド**で±1%台、将来4点指定で±0.5%まで。

---

## 3. 時間分解能の限界突破(E2)

ブラウザの`getUserMedia`は実質30〜60fpsまで。これが土俵の限界。突破口は2つ。

### 3a. 統計で稼ぐ(リアルタイムモード)

- 飛行0.45秒 × 60fps = 約27サンプル。多点回帰で実効分解能は1フレームの 1/√27 ≈ 1/5
- **サブピクセル重心**: ボールのモーションブラー筋の輝度重心は0.1px精度で取れる。解析解像度を160→320pxへ(WebWorker化で処理を逃がす)
- **物理モデルでフィット**: x(t)を直線ではなく**抗力つき減速モデル** x(t) = (1/k)·ln(1+k·v₀·t) でフィットすると、回帰そのものがリリース初速v₀を直接推定する。換算誤差も消える
- 期待精度: **±1%(130km/hで±1.3km/h)**

### 3b. スローモーション撮影を取り込む(公式計測モード)

iPhoneは240fps、3万円のAndroidでも120fpsのスロー撮影ができる。**標準カメラで撮ったスロー動画をその場でアプリが自動解析**する第2モード:

- ユーザー操作は「カメラアプリでスロー撮影 → 共有ボタンからこのアプリへ」の2手
- 240fpsなら1球で100サンプル超 → **±0.3%(±0.5km/h)**。市販レーダーガンと同等
- リアルタイムの楽しさは损なわない。**「練習はリアルタイム、記録会はスロー解析」**と役割を分ける
- 公認記録・スカウト向けクリップはこちらで生成(動画自体が証拠になる)

---

## 4. 検出の頑健化(E4)

- **背景安定化**: 手ブレは「除外」ではなく「補正」する。背景の特徴点(コーナー)を毎フレーム追跡し、フレーム間の平行移動を推定して差分前に位置合わせ → 手持ち撮影でも計測可能に
- **RANSAC外れ値除去**: 回帰サンプルから「直線+減速モデルに乗らない点」(鳥・人・旗)を自動排除
- **時空間整合性**: ボール候補は「前フレームの位置から物理的に可能な範囲」にしか現れない(カルマン的ゲーティング)。背景の無関係な動きをほぼ全部落とせる
- **露出ロック**: `applyConstraints({exposureMode:'manual'})` 対応端末では自動露出の変動ノイズを止める
- 将来: 軽量CNN(WASM, 〜1MB)でボール検出。ただし古いスマホで動くことが最優先なので、まずは古典CVを限界まで

---

## 5. 古いスマホこそ基準機(性能設計)

「貧乏な子のスマホで動く」は機能要件ではなく**性能要件**:

- 解析はWebWorker + 縮小グレースケール。目標: **2015年級SoCで30fps処理落ちゼロ**
- 通信ゼロで完結(PWA・オフライン)。ギガを消費しない
- 電池: 連続計測30分でバッテリー10%以内(解析解像度の動的調整)
- 30fps端末では自動的に「サンプル数を増やす」(計測区間を広く取る)方向に最適化

---

## 6. 信頼性の設計(数字を社会に通用させる)

- **誤差を常に表示**: 回帰の残差からその1球の95%信頼区間を計算し「136.2 ±1.1 km/h」と出す。盛らない設計こそ公器の条件
- **検証プロトコル**: 市販レーダーガンと並走100球 → 誤差分布・相関をリポジトリで公開。バージョンごとに再検証
- **証拠クリップ**: 計測瞬間の動画に球速・信頼区間・日時・キャリブレーション情報を焼き込んで保存。記録は「言い値」でなく「見せられるもの」に
- **不正耐性**: キャリブレーション点と映像が幾何的に矛盾していないか(プレートの見かけサイズ vs 申告距離)を自動検査

---

## 7. ロードマップと到達精度

| フェーズ | 内容 | 期待精度(130km/h時) |
|---|---|---|
| 済 | 多点回帰 + 初速換算 + 揺れ除去 | ±3〜5 km/h(距離系統誤差は残存) |
| **P1** | 2点タップキャリブレーション(E1根治) + 物理モデルフィット | ±2 km/h、系統誤差ほぼゼロ |
| **P2** | 背景安定化 + RANSAC + サブピクセル + WebWorker化 | ±1.3 km/h |
| **P3** | スロー動画の公式計測モード(120/240fps自動解析) | **±0.5 km/h = 市販ガン同等** |
| **P4** | 信頼区間表示 + 実機検証データ公開 + 証拠クリップ | 「正確」から「信用される」へ |
| P5 | 4点ホモグラフィ / 露出ロック / CNN検出 / 2台同期ステレオ | 研究枠 |

**P1が最優先**(最大の系統誤差を式の変更だけで根治できる、コスパ最強)。
P3が「最強」の本体 — 子供のスマホに既に載っている240fpsカメラを解放する。

---

## 8. 一球の処理パイプライン(P2完成時の姿)

```
カメラフレーム (rVFC, mediaTime)
  → 縮小グレースケール (WebWorker)
  → 背景特徴点で手ブレ補正
  → フレーム差分 + 時空間ゲートでボール候補抽出
  → サブピクセル重心 (t, x, y)
  → キャリブレーション行列で地面座標(m)へ変換
  → RANSAC + 抗力モデル x(t)=(1/k)ln(1+k·v₀·t) フィット
  → v₀ (リリース初速) + 95%信頼区間
  → 表示・記録・証拠クリップ生成
```
